global:
  scrape_interval: 5s
  evaluation_interval: 5s
  external_labels:
    environment: loadtest
    service: imhere

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: prometheus
    scrape_interval: 15s
    static_configs:
      - targets: ["prometheus:9090"]
