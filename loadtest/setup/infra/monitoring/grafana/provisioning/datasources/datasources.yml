apiVersion: 1

datasources:
  - name: loadtest-prometheus
    uid: loadtest-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: loadtest-loki
    uid: loadtest-loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false

  - name: loadtest-tempo
    uid: loadtest-tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
    editable: false
